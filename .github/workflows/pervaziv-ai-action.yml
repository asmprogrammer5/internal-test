name: Pervaziv AI Code Review

on:
  push:
    branches:
      - main              # runs on every push to main
  schedule:
    - cron: '4 * * * *'  # runs every day at midnight UTC

jobs:
  pervaziv-ai-action:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: write
      pull-requests: write
    steps:
      - uses: pervaziv/pervaziv-ai-code-review@Fix_1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          console-url: https://console.pervaziv.com
